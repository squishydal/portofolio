{
  "name": "terminal-resume",
  "version": "2.0.0",
  "description": "Interactive terminal-style resume with enhanced features",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "gh-pages": "^6.1.0"
  },
  "dependencies": {
    "xterm": "^5.3.0",
    "xterm-addon-fit": "^0.8.0",
    "jspdf": "^2.5.1",
    "highlight.js": "^11.9.0"
  }
}
